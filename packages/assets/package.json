{
    "name": "@passplum/assets",
    "version": "2.0.0",
    "description": "static assets for web",
    "scripts": {
        "start": "serve ./src",
        "test": "echo 'No tests for static assets' && exit 0",
        "deploy": "pushd src/ && now --static --public --name passplum-assets && popd"
    },
    "devDependencies": {
        "serve": "^6.0.6"
    }
}
